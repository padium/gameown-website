enum Path {
    HOME = "/",
    LOGIN = "/auth",
    REGISTER = "/auth/register",
    DASHBOARD = "/dashboard",
    STORE = "/store",
    COMMUNITY = "/community",
    INVENTORY = "/inventory",
    MARKETPLACE = "/marketplace"
}

export default Path