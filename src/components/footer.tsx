import {Container} from "@mui/material"

export default function Footer() {
    return (
        <footer className="fixed bottom-0 w-full">
            <Container>Hi im a footer</Container>
        </footer>
    )
}
